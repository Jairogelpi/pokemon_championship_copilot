# Application API

Planned FastAPI service for match lifecycle, persistence, AI event proposals,
and decision orchestration. It will call the battle engine through a typed
boundary and will never store an independent copy of canonical battle state.
